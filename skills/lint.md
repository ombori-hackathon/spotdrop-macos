# Lint Skill

Run SwiftLint on macOS code.

## Usage

```
/lint
```

## Commands

```bash
cd spotdrop-macos
swiftlint
```

## Prerequisites

Install SwiftLint:
```bash
brew install swiftlint
```

## Configuration

Create `.swiftlint.yml` for custom rules.
